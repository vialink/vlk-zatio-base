FROM mysql:5.7

ARG PROJECT_NAME=zatio_demo
ARG USER=admin
ARG USER_PASS=admin
ARG DB_NAME=wms_${PROJECT_NAME}
ARG ROOT_PASS=admin

ENV MYSQL_ROOT_PASSWORD=${ROOT_PASS}
ENV MYSQL_DATABASE=${DB_NAME}
ENV MYSQL_USER=${USER}
ENV MYSQL_PASSWORD=${USER_PASS}

EXPOSE 3306

CMD ["mysqld"]
